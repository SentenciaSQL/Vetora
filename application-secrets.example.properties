# Copy to application-secrets.properties (never commit the real file).
# Used by Spring via: spring.config.import=optional:file:./application-secrets.properties

PADDLE_API_KEY=
PADDLE_ENVIRONMENT=sandbox
PADDLE_WEBHOOK_SECRET=
PADDLE_CLIENT_TOKEN=
PADDLE_GRACE_PERIOD_DAYS=10

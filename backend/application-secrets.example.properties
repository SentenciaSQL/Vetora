# Copy to application-secrets.properties in the working directory used to start the API.
# Never commit real secrets.

PADDLE_API_KEY=
PADDLE_ENVIRONMENT=sandbox
PADDLE_WEBHOOK_SECRET=
PADDLE_CLIENT_TOKEN=
PADDLE_GRACE_PERIOD_DAYS=10
